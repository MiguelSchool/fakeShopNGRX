import { OnFilterCategoriesPipe } from './on-filter-categories.pipe';

describe('OnFilterCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new OnFilterCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
